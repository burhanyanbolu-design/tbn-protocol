server {
    listen 80;
    server_name tbn.hardinai.co.uk;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";

    # Proxy to TBN Flask app (port 5004)
    location / {
        proxy_pass         http://127.0.0.1:5004;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:5004/api/stats;
    }
}

# HTTPS block — uncomment after running certbot
# server {
#     listen 443 ssl;
#     server_name tbn.hardinai.co.uk;
#     ssl_certificate     /etc/letsencrypt/live/tbn.hardinai.co.uk/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tbn.hardinai.co.uk/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#
#     location / {
#         proxy_pass         http://127.0.0.1:5004;
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
